.claude
__pycache__

# generated model weights + token memmaps (rebuild on the VM)
*.pt
*.bin
corpus_sample.txt
custom_bpe*

# old toy data
training-data.json

# SECRETS — never commit
gpus.md
*.env
.env
secrets*
# agent/verifier scratch + modal logs
modal_run.log
verify/_runs/
