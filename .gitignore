# --- Context / IDE ---
.claude
__pycache__/
*.pyc
.DS_Store

# --- Hackatime heartbeat state ---
.hackatime-last-check

# --- Generated model weights + token memmaps (rebuild on the VM) ---
*.pt
*.bin
corpus_sample.txt
custom_bpe*
rft_data.json

# --- Old toy data (archived in models/) ---
training-data.json

# --- SECRETS — never commit ---
gpus.md
*.env
.env
secrets*

# --- Runtime scratch ---
modal_run.log
verify/_runs/
eval/_runs/

# --- Python ---
*.egg-info/
dist/
build/
.eggs/
*.egg

# --- Notebook checkpoints ---
.ipynb_checkpoints/
# Stale data memmaps (generated by data pipeline, can be regenerated)
*tokens_*.bin

# Verifier temp files (generated during evaluation)
verify/_runs/*.py
training/
.pi-glla/

# Root-level notebook copies (use notebooks/ dir)
chaton_*.ipynb
kaggle/kaggle_train_upload.py
kaggle_output/le-gros-chaton
kaggle_output2/le-gros-chaton
eval/.tb_jobs/
.agents/
qwen_traj_sft_flat/
qwen_traj_sft_verify/
agent_traces_normalized.jsonl
eval/.tb_jobs_pilot/
eval/tb_traces/
research/
eval/.tb_cache/
