# --- Context / IDE ---
.claude
__pycache__/
*.pyc
.DS_Store

# --- Generated model weights + token memmaps (rebuild on the VM) ---
*.pt
*.bin
corpus_sample.txt
custom_bpe*
rft_data.json

# --- Old toy data (archived in models/) ---
training-data.json

# --- SECRETS — never commit ---
gpus.md
*.env
.env
secrets*

# --- Runtime scratch ---
modal_run.log
verify/_runs/
eval/_runs/

# --- Python ---
*.egg-info/
dist/
build/
.eggs/
*.egg

# --- Notebook checkpoints ---
.ipynb_checkpoints/
# Stale data memmaps (generated by data pipeline, can be regenerated)
*tokens_*.bin

# Verifier temp files (generated during evaluation)
verify/_runs/*.py
training/
.pi-glla/

# Root-level notebook copies (use notebooks/ dir)
chaton_*.ipynb
kaggle/kaggle_train_upload.py
